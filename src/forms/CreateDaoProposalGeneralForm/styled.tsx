import styled from "styled-components/macro"

import { AppButton } from "common"

export const StepsRoot = styled.div`
  display: flex;
  flex-direction: column;
  transform: scale(1);
  gap: 16px;
  padding: 14px 16px 20px;
`

export const SuccessModalButton = styled(AppButton)`
  width: 100%;
`
