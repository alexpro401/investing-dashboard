import styled from "styled-components/macro"

import { MediumText } from "common"

export const SectionTitle = styled(MediumText).attrs(() => ({
  size: "20px",
  weight: 900,
  color: "#6781BD",
}))`
  line-height: 25px;
`
