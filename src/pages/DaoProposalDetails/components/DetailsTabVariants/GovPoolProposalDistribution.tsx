import { FC, HTMLAttributes } from "react"

interface Props extends HTMLAttributes<HTMLDivElement> {}

const GovPoolProposalDistribution: FC<Props> = ({ ...rest }) => {
  return <></>
}

export default GovPoolProposalDistribution
